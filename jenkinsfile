pipeline {
    agent any

    stages {
        stage('pull') {
            steps {
                git branch: 'main', url: 'https://github.com/iamtruptimane/node-js-app-CICD.git'
            }
        }
         stage('Build') {
            steps {
                echo 'Build-job'
            }
        }
       
        stage('test') {
            steps {
                echo 'test-job'
            }
        }
         stage('deploy') {
            steps {
                echo 'deploy-job'
            }
        }
    }
}
